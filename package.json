{
  "name": "croft-connect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Croft Exchange lookup page (web) — resolution pipeline and unit tests.",
  "scripts": {
    "test": "vitest run",
    "mutate": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "vitest": "^2.1.9"
  }
}
