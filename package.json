{
  "name": "croft-connect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Croft Exchange lookup page (web) — resolution pipeline and unit tests.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
